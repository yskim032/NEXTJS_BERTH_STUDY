'use client';

import BCTClient from './BCTClient';
import { useEffect } from 'react';

export default function BCTPage() {
  useEffect(() => {
    // 여기에 필요한 로직 추가
  }, []);

  return <BCTClient />;
}

