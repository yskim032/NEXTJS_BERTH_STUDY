'use client';

import BCTClient from './BCTClient_study';
import { useEffect } from 'react';

export default function BCTPage() {
  useEffect(() => {
    // 여기에 필요한 로직 추가
  }, []);

  return <BCTClient />;
}

